{# {{namehtml}} #}

  <p>Détails de l'auteur</p>
    <h1>{{ author.username }}</h1>
    <img src="{{ asset(author.picture) }}" style="height: 100px; width: 100px;">
    <p>Email : {{ author.email }}</p>
    <p>Nombre de livres : {{ author.nb_books }}</p>

{#name : {{author.username}} <br>
email: {{ author.email }} <br>
image : <img src="{{ asset(author.picture) }}" style="height: 100px; width: 100px;"> <br>
Nombre de livres :  {{ author.nb_books }} #}